launch:
  - arg:
      name: board_name
      default: "large"
      description: "Name of ChArUco board to track"
  - node:
      package: chess_vision
      executable: charuco_tracker_node
      name: charuco_tracker
      parameters:
        - board_type: "$(var board_name)"
